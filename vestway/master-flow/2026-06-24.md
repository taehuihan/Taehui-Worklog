# 작업 로그 — 2026년 6월 24일 (내정보 재빌드 + connect() 빌더 + 커넥터 가이드 감사)

> 작성자: 태휘 · 앱: Vestway · 테스트 파일 `TCFQPUYWPmBmwcpo7cCu8w`

---

## 한 일

### 내정보 플로우 재빌드 (`16297:15585` 삭제 → `16397:22510`)
- 가이드 검증 테스트: 소스 `9123:23931`(내정보 페이지)에서 구조·트리거 재파악 후 재생성.
- **섹션 위치 규칙 = 직전 섹션 우측 +500** (챌린지 우측 x58916… 후 챌린지를 x46336으로 옮기며 규칙도 500 확정).
- **행이 뎁스 가로질러 정렬**(구독하기↔약관안내 / 이용권내역↔구독해지 / 내정보수정↔수정항목), 행 y = 직전 행 전체 max bottom + 837.
- 기존 오류 교정: notice 확인모달 2개를 구독해지 행 → **약관·안내 행**으로.

### 커넥터 트리거 실수 연쇄 → `connect()` 빌더로 근본 해결
손좌표 하드코딩 탓에 반복 실수: ①트리거 findOne 오인(무료체험관리하기↔관리하기) ②프레임 모서리 출발 ③형제 화면 관통 ④다중출발 오해(맨우측 몰아넣기) ⑤팬아웃 레인 미분리.
→ **`connect(출발화면, 트리거질의, 도착화면)` 빌더** 제작:
- 트리거 자동탐색(**전체 구문 매칭** + 컴포넌트/링크 우선) → 컴포넌트 모서리 출발
- **모든 화면 bbox 충돌검사** → 관통0 경로만 채택(자동 위/아래 우회)
- 도착 = 화면 좌변 중앙, **팬아웃 레인 분리**(도착순 분산)
- 못 찾으면 날조 대신 **실패 보고**
- 내정보 커넥터 9개를 빌더로 재작도, 기하검증 전부 `crosses: none`.

### 커넥터 가이드 감사 → 6건 정정 (SSOT + guide.md)
1. 출발점 소제목 "중심 좌표" → **모서리**(고정 모델과 일치)
2. 트리거 후보 필터 "텍스트 제외" → **스타일 텍스트 링크는 트리거**(본문/배경만 제외) ← 활성 버그
3. **다중 상태 화면 조건부 출발** 규칙 추가(맨우측 1개 vs 각 상태 버튼)
4. **트리거 선별**: findOne 첫매치 금지 → 전체구문 매칭
5. **같은 행 형제 화면도 화면** → 위로 우회
6. 구버전 §6 충돌 → 상단 폐기 배너가 커버(DEPTH_GAP 500·행폭한계). mkVec stroke는 이미 0.478.

## 동기화
- Figma SSOT `16097:3930`: §5 소제목·후보필터·다중상태·트리거선별·형제화면 정정(캐스케이드)
- guide.md 현행 §5/§6 동일 반영
- 메모리 `master-flow-section-guide`: 정정 6건 + `connect()` 빌더 기록

## v2 비교 테스트 (가이드 검증)
업데이트된 가이드가 소스로부터 올바른 플로우를 재현하는지, **v1 옆에 v2를 새로 빌드**해 검증.
- **내정보 v2 `16424:22510`**(v1 우측 x65966): 소스 재독→공식 레이아웃→빌더. 9커넥터 전부 6규칙 통과. v1과 결정적 일치 = 재현성 확인.
- **챌린지 v2 `16427:24633`**(내정보v2 우측 x73016): 32화면 재현. **버그 발견** — `arrow_right` 동명 ×3 → 빌더가 내상금 트리거로 *전체랭킹 행* arrow(cy2084)를 오선택(상금 행 1988 아님). 두 커넥터가 같은 자리서 출발.
  - **핵심 교훈**: 기하검증(관통·도착·꺾임)은 전부 통과 → **수치검증은 트리거 *의미* 오류를 못 잡는다.** 소스 네이티브 커넥터/시각 대조 필수.
  - 수정: "누적 상금" 라벨 근접 arrow로 특정 → (1150,1988). 규칙화: 동명 아이콘은 **행 라벨로 행 특정 후 우측 아이콘**(라벨-아이콘 세로 분리 행은 행 컨테이너 우측).
  - SSOT §5 트리거 노트 + guide.md §6 반영.

## connect() 빌더 정본 파일화 (완료)
- **`connect-builder.js`** 생성 — 매번 인라인 재구성하던 빌더를 단일 정본 파일로. 사용법: Read로 읽어 use_figma에 verbatim 주입(require 불가).
- **검증으로 findTrigger 개선 발견**: 첫 정본(행컨테이너 우선)은 챌린지 상금 행(라벨-arrow 49px 오프셋)에서 텍스트 모서리(936)로 폴백 = 결함. → **"라벨 cy 최근접 우측끝 아이콘(±65)"** 으로 개선.
- 양쪽 섹션 재검증 통과: 챌린지 v2 내상금 자동으로 (1142,1988) 정확 / 내정보 v2 회귀 없음(구독해지 링크=자기우측 2091 유지, 촘촘한 언어/알림 구분).
- 미검증 정본을 커밋하지 않고 **실행 검증 후 확정**한 게 핵심.

## 다음
- selling→약관안내: 소스에 트리거 없음 → 진입 경로 확인 필요
- v3(V.AI) DEPTH_GAP 800 재배치
- 빌더 정본을 다른 플로우(전체)에 점진 적용
