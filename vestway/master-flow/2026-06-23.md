# 작업 로그 — 2026년 6월 23일 (내정보 커넥터 정리 + 라우팅 규칙)

> 작성자: 태휘 · 앱: Vestway · 테스트 파일 `TCFQPUYWPmBmwcpo7cCu8w`

---

## 한 일
내정보 플로우(`16297:15585`) 커넥터를 다듬으며 **라우팅 관련 규칙**을 정립, SSOT·git·메모리 동기화.

### 커넥터 라우팅 문제 2건 해결
1. **화면 겹침** — 커넥터가 화면 프레임 위/가장자리에 겹침
   → **라우팅 레인 규칙**: 화면은 섬, 커넥터는 갭으로만. 출발·도착만 화면 접촉. 수직=화면 사이 갭 안쪽, 수평 우회=화면 top보다 위(레이블 밴드). (subscription→cancel, my→selling edge-run 수정)
2. **커넥터 몰림** — my 팬아웃 리저들이 좁게 뭉침
   → 갭 폭 활용해 리저 간격 30→100px, **도착 행 순서로 레인 배정**해 교차 최소화

### DEPTH_GAP 500 → 800 (새 표준)
- down-lane(세로 리저)과 화면 간격이 좁다는 피드백 → **DEPTH_GAP 800으로 재배치**(D2 +300, D3 +600, 커넥터 9개 재작도)
- **앞으로 모든 플로우 작업은 DEPTH_GAP=800** (SSOT·메모리 기록). 이전 500 폐기.

### SSOT 커넥터 규칙 완비 (섹션 5)
뎁스 간 커넥터 / 트리거 인식 / 자동 트리거 매칭 / **라우팅 레인** / **확인 모달** — 커넥터 관련 전부 SSOT에 기록.

## 동기화
- Figma SSOT `16097:3930`: DEPTH_GAP 800, 라우팅 레인 노트, 확인 모달 노트 추가
- git `guide.md`: 현행 모델에 DEPTH_GAP 800 + 7)자동매칭 8)라우팅 9)확인모달 추가
- 메모리 `master-flow-section-guide`: DEPTH_GAP 800 + 라우팅/확인모달 반영

## 다음
- v3(V.AI) 섹션은 아직 DEPTH_GAP 500 — 800으로 재배치할지 결정
- 내정보: edit 하위 추가 커넥터(비번·번호), 확인 모달 2개 행선지 구분

---

# 2부 — 챌린지 모바일 플로우 재구축 + 가이드 작업순서 가드

## 한 일

### 챌린지 섹션 재구축 (`16351:22109`)
- 기존(16333:19659) 삭제 후 **트리거 맵 우선 워크플로**로 재빌드. 소스 `2764:4613`(챌린지 페이지)에서 32화면 클론.
- **누적상금내역 15화면 전부 포함**(이전엔 핵심 3개만). 네이밍=뎁스로 D3에 계좌등록(8상태)·대상자안내(4) 분리.
- 트리거 맵 = 소스 **네이티브 커넥터 endpoint** 읽어 요소→도착 매핑(홈 카드→종목선택, 전체랭킹행→전체랭킹, arrow→내상금내역, button_square→주가예측). 홈→안내는 네이티브 트리거 없어 생략(이전엔 추론으로 그렸던 것).

### 가이드 작업순서 가드 (SSOT 16097:3930) — 누락 재발 차단
- **0단계 트리거 맵** 추가(체크리스트 맨 앞). **4·5·6단계 §2 포인터**(링크만, 공식은 §2). **9단계 수치 자기검증**(행 837/DEPTH_GAP 800 assert).
- 근본 원인: *규칙이 가이드에 있어도 작업 순서에 박혀있지 않으면 누락* → 트리거 매칭·행간격 둘 다 같은 뿌리. 포인터(예방)+검증(탐지) 2층.

### 누락 사례 3건 잡고 규칙화
1. **트리거 매칭 누락** — 챌린지 빌드 때 임의 y로 그림 → 트리거 메뉴 행 실제 y로 보정. (가이드 §7 자동 매칭은 있었으나 작업순서 미반영이 원인)
2. **행간격 +600 (가이드는 +837)** — §2 공식(그룹간격 400 + 라벨 437) 미적용 → 하위 5개 행 재배치. → §2 포인터·9단계 검증으로 가드.
3. **출발점 모델 불일치** — 내정보=컴포넌트 모서리(X·Y), 챌린지=프레임 모서리(Y만). → **내정보 모델로 고정**(§5 명문화, 팬아웃 편의 금지). 챌린지 홈 팬아웃 1170→1150 보정.
4. **레인이 모달 관통** — 내상금 레인 x1600이 D1 모달행 결과모달(x1159–1865) 관통 → **인터밴드 갭 x3150**으로 이동. → §8 "레인 전구간 클리어런스" 규칙 추가.
5. **도착점 규칙 없음** — 출발점만 고정 모델 박고 도착점은 무규칙 → 챌린지 도착이 화면 상단(screen_y+200)으로 제각각 → **도착=도착 화면 좌변 세로 중앙** 명문화(§5, 출발↔도착 대칭), 챌린지 5개 도착점 center 보정(종목선택1779·전체랭킹3469·내상금5199·계좌등록1733·대상자안내3382), 9단계 검증에 `도착 y==center` 추가.

## 동기화
- Figma SSOT `16097:3930`: 0/9단계, §2 포인터, §5 출발점 고정 모델, §8 레인 클리어런스 (캐스케이드로 높이 확장)
- git `guide.md` 현행 모델: 출발점 고정 모델·레인 클리어런스·챌린지 섹션·작업순서 가드 추가
- 메모리 `master-flow-section-guide` + 신규 `feedback_rule_in_worksteps`

## 다음
- v3(V.AI) 섹션 DEPTH_GAP 800 재배치 여부
- 챌린지: 홈→안내 표기 방식(추론 커넥터 OVER 라우팅 검토)
- 9단계 수치검증을 빌드 스크립트에 실제 assert로 내장
