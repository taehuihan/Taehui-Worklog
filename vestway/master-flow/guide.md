# Task 페이지 → Master 페이지 플로우 자동화

> 작성자: 태휘  
> 작업 기간: 2026년 6월  
> 대상: Vestway 앱 디자인 시스템 — V.AI 화면 플로우를 Master 다이어그램 페이지에 자동 정리

---

## ⭐ 현행 모델 (v3 + 챌린지, 2026-06-24 갱신) — 이 섹션이 최신·우선

> 아래의 옛 섹션들(5-Depth 고정, "행 폭 한계 규칙", `INTERNAL_GAP=40/20`, Task 페이지 등)은
> **구버전 기록**이다. 현재 기준 = 본 섹션 + Figma SSOT(`16097:3930`).

### 1) 뎁스 = 프레임 네이밍의 `/` 깊이
- `vai/onboarding`=D1 · `vai/tutorial`=D2 · `vai/home`=D3 · `vai/home/*`=D4
- `vai/home/` 아래(대화·입력·추천질문·이용약관·구독·출처·복사·북마크·신고·신고접수·검색·블룸버그)는 **전부 같은 뎁스(D4)**
- 이유: **모달·같은 화면 내 동작은 "페이지 이동"이 아니므로 뎁스가 늘어나지 않음**
- 모달은 그 화면과 **같은 뎁스**. 예) 블룸버그 모달은 home(D3)에 뜨므로 D3, chat 모달들은 chat(D4)이므로 D4

### 2) 행 배치
- **chat 본체·상태(대화·입력·추천질문) = 같은 행** (같은 화면의 상태)
- **모달은 같은 `/chat`이라도 별도 행**으로 분리
- 한 뎁스 안에서 여러 행 가능, **한 행 화면 수 제한 없음** (밴드가 콘텐츠 폭으로 늘어남)

### 2-1) 레이블 모델 (기준섹션 16037 실측 — ⚠️ 의미 혼동 주의)
- **서브레이블 = 뎁스(열)별 콘텐츠 그룹명**. 화면이 있는 **각 뎁스 열 시작 x**에, 행이 여러 개면 행마다(y 다르게) 단다. 예) D1 "인텔리전스 홈" / D2 "설명"·"상세" / D3 "대화". **「유저 세그먼트(무료 유저/유료 유저)」나 「행 그룹명」으로 쓰지 말 것** — D1에만 붙는 오류 발생(블룸버그, 2026-06-26).
- **상태레이블 = 화면별 상태명**(무료/유료/유료 CTA/태블릿 등), 각 화면 x에 나란히. **단일 화면(상태 안 나뉨)은 상태레이블 생략**(기준섹션 설명·대화엔 없음).
- **같은 화면의 다른 상태(무료/유료) = 같은 행 나란히** + 상태레이블로 구분. **다른 화면 = 다른 행**(분기행, §5). 둘은 별개 축: 상태는 가로(상태레이블), 화면 차이는 세로(행).

### 3) 수평 상수 (갱신)
- `SCREEN_WIDTH = 375` · **`INTERNAL_GAP = 60` (step 435)** ← 구문서 20/40은 폐기
- **밴드 폭 = max(435×N − 60, 810)** (2화면=810 · 5화면=2115 · 7화면=2985)
- **`DEPTH_GAP = 800`** (밴드 끝 → 다음 밴드 시작. 커넥터 레인 여유 확보, 2026-06-23~. 이전 500 폐기)

### 4) 수직 상수 (유지)
- row0: 밴드 y=360(h130) → 서브레이블 y=890(h58) → 상태레이블 y=1113(h44) → 화면 y=1327
- 다음 행: **서브레이블 y = 이전 행 화면 bottom + 400**, 상태레이블 = 서브+223, 화면 = 서브+437

### 5) 커넥터 규칙 (갱신)
- **커넥터는 뎁스 간 이동(navigation)만 표기.** 같은 뎁스 내(같은 행·다른 행) 화면끼리는 **연결 안 함** — 모달·상태는 나열만
- **조건 레이블(분기 조건 텍스트·다이아몬드) 표기 안 함**
- **출발점 = 고정 모델 (내정보 방식, 항상 이것만 사용)**: 트리거 컴포넌트 bbox를 읽어 **그 모서리(X·Y 모두)** 에서 출발. 화면 1개든 팬아웃이든 동일. **금지**: 팬아웃에서 "프레임 모서리에 정렬, Y만 트리거에서" 같은 편의 — X도 반드시 컴포넌트 기준. 내부 트리거는 컴포넌트 모서리에서 출발 → 최근접 프레임 모서리로 빠진 뒤 라우팅(Y=컴포넌트 center)
- **도착점 = 도착 화면 좌변의 세로 중앙** (다중 도착은 중앙 기준 ±20px 분산). 출발=컴포넌트 모서리 ↔ 도착=화면 좌변 중앙 (대칭). 화살표 `ARROW_EQUILATERAL`
- **다중 상태 화면 출발 (조건부)**: 트리거→도착이 상태 불문 같음 → **맨 우측 화면 1개** / 상태별 도착이 다름(다중출발) → **각 상태 화면 버튼에서 각각** (구버전 "무조건 맨우측"은 폐기. 무료/무료체험 사례)
- 화면 통과 금지 (OVER 우회 / midX 분리). **같은 행 형제 화면도 화면** — 트리거가 행 왼쪽이면 형제들 위(레이블 밴드)로 우회 (정기구독중→해지 사례)
- **첫 방향전환 = 화면 우측 +20px 이후** (`firstBend_x ≥ 트리거화면.right + 20`): 출발 후 트리거 화면을 **벗어나서** 꺾는다. 화면 안/가장자리(+20 전)에서 꺾지 않음. (구버전 +10 삭제했다가 **+20로 부활** — 화면 이탈을 깔끔히. `connect()` 빌더가 강제·검증)

### 6) 트리거 인식 (커넥터가 화면 통째에 붙어 있을 때)
- **트리거는 화면이 될 수 없다** → 무조건 가장 가까운 아이콘/버튼
- 커넥터의 **실제 bbox 시작점**(화면에 닿는 끝점)을 구해, 그 점을 **포함(거리 0)** 하거나 **최근접**하는 인터랙티브 컴포넌트 = 트리거
- **magnet(AUTO) 값으로 추정 금지** — 실제 렌더된 bbox를 봐야 정확 (AUTO는 타겟 방향 가장자리로 빠져 빗나감)
- 후보 필터: INSTANCE/COMPONENT·버튼·아이콘·**스타일된 텍스트 링크** (배경·본문 텍스트만 제외 — 링크 텍스트는 트리거 가능)
- 전제: 커넥터를 트리거 근처에서 시작해 그려야 정확
- **트리거 선별**: `findOne` 첫매치 금지 — **전체 구문 매칭**(부분매칭 ✗) + INSTANCE/링크 우선으로 정확히 1개 특정 ("무료 체험 관리하기"를 "관리하기"로 오인한 사례)
- **메뉴행 트리거 = 행 우측 아이콘**: 라벨 텍스트 모서리에서 출발 ❌ → 라벨로 행을 **식별**만 하고, 출발은 **그 행 우측의 아이콘/요소 bbox 우측**(셰브론 등). 소스 커넥터가 출발한 위치의 요소를 그대로 잡는 것과 동일. 독립 카드/링크는 그 자신 우측. (언어/알림이 텍스트 x867이 아니라 아이콘 x1150에서 출발해야 하는 사례)
- **모호한 아이콘 이름 질의 금지**: `arrow_right` 등 한 화면에 여러 개면 findTrigger가 엉뚱한 걸 잡음 → **행 라벨 텍스트로 행을 특정**한 뒤 그 행 우측 아이콘. 라벨-아이콘이 세로로 떨어진 행(상금 행 등)은 라벨 근접 아이콘/행 컨테이너 우측. ⚠️ **수치검증(관통·도착·꺾임)은 트리거 *의미* 오류를 못 잡음** → 소스 네이티브 커넥터·시각 대조 필수. (챌린지 v2 테스트: arrow_right ×3 → 내상금이 전체랭킹 행 arrow를 잡은 사례)

### 트리거 맵 추출 = 위·아래 양방향 (누락 0 설계)
커넥터만 따라가면(위→아래) *커넥터 자체가 빠진 전환*(예: 챌린지 안내가 소스에 없던 때)을 영영 놓침. **버튼 전수(아래→위)** 가 안전망.
1. **소스 커넥터 전수** 읽기 — 모든 홈 상태·일부만 ❌·position 기반도 포함
2. **출발점 → 리프 트리거** — 출발이 컨테이너(카드)면 **도착 라벨로 안쪽 버튼 특정** (card→결과확인 ⇒ "결과 확인" 버튼, card→종목선택 ⇒ "수정하기" 버튼)
3. **버튼 전수 교차검증** (`crossCheckTriggers`, connect-builder.js):
   - **커버**(자기/조상(카드)/자손이 커넥터 출발) → 자동 ✓ (검토 X)
   - **제외**(per-flow 제외목록: 탭바·토글·조건부모달 등) → 자동 (검토 X)
   - **⚠️검토**(나머지) → 사람이 **의심 소수만** "연결/제외" 1회 판정. 제외는 목록에 기억 → 다음엔 자동
   - 부담 = "전수 검토" ❌ → "의심 소수 1회 판정" ✅. 전수 열거하되 자동 분류로 검토량 최소화.

### 7) 자동 트리거 매칭 (커넥터를 새로 그릴 때)
- 소스 화면 안의 **행/요소(메뉴행·버튼·카드)의 텍스트·이름을 스캔**
- 커넥터 목적지(도착 화면)와 **의미 매칭** → 그 요소의 우측/행 y에서 출발시킴
- 예) "이용권 내역" 메뉴행 → subscription 화면 / "내 정보 수정하기" → edit ("내정보 플로우"에서 적용)

### 8) 라우팅 — 화면 겹침·몰림 방지
- **화면은 섬, 커넥터는 갭(바닷길)로만.** 출발·도착만 화면에 닿고 중간 꺾임은 갭 안에서
- **수직** = 화면 사이 갭 안쪽 레인(가장자리에 붙이지 말 것) · **수평 우회** = 화면 top보다 위(레이블 밴드)
- **1→다 팬아웃**: 갭 폭 활용해 리저 간격 벌리고(예 100px) **도착 행 순서로 레인 배정** → 교차 최소화. 좁으면 `DEPTH_GAP` 확대
- **레인 전구간 클리어런스**: 세로 레인 X는 **하강하며 지나는 모든 행**에서 화면과 안 겹쳐야 함. 출발 높이의 가로 여백만 보면 아래 행 화면을 관통함. 다른 행을 통과하는 깊은 하강은 **밴드 안쪽 ✗ → 인터밴드 갭(밴드 끝~다음 밴드 시작)으로** 라우팅. (사례: 챌린지 내상금내역 레인 x1600이 D1 모달 행 결과모달 관통 → x3150 갭으로 이동)

### 9) 확인 모달 (Basic dialog: 확인/취소)
- 확인 모달은 네이밍 없어도 **그것을 띄운 화면의 뎁스(모달 행)** 에 배치
- **부모 화면 ↔ 모달 커넥터 없음** (같은 뎁스, 모달은 나열). 어디서 떴는지는 트리거 버튼 위치로
- **"확인"의 행선지(결과 화면)만 커넥터** — 출발 = 모달의 확인 버튼. "취소"(부모 복귀)는 표시 안 함

### v3 섹션 (현행)
- **`16245:20440`** ("V.AI 개선 플로우 v3", Master 페이지) — 소스 `V.ai 개선`(`16189:41312`, V.AI 페이지)에서 화면 새로 클론, 네이밍 기준 정리
- 구조: **D1 온보딩 / D2 튜토리얼 / D3 홈(+블룸버그 모달 행) / D4 home하위 전체**(대화하기 · 모달/액션 7화면 · 검색하기 5화면)
- 뎁스 간 커넥터: `onboarding/2→tutorial/1`, `tutorial/2→home`, `home→chat`, `home→search` (home→bloomberg는 같은 뎁스라 커넥터 없음)

### 챌린지 섹션 (현행, 모바일)
- **`16374:19659`** ("챌린지 플로우 (모바일)", Master 페이지, **내정보 우측 x46336**) — 소스 `챌린지`(`2764:4613`)에서 32화면 클론. **트리거 맵(네이티브 커넥터 endpoint) 먼저 읽어** 뎁스·출발점 판단. 처음부터 재빌드(2026-06-23), 9단계 수치검증 통과(행 837·도착 center·DEPTH_GAP 800). 참여(종목선택·주가예측·결과확인)는 **1행 유지**(사용자 결정 — 엄격 네이밍이면 3행이나 참여 플로우로 묶음)
- 구조: **D1 챌린지홈**(상태 시작전·후 + 모달 4: 시작·결과·리워드안내·구독유도) / **D2** 참여(종목선택·완료·확인·예측1·2·확인·이탈모달·결과확인·결과상세 = 한 행)·전체랭킹·내상금내역(내역·계좌미등록·계좌등록완료)·챌린지 안내 / **D3** 계좌등록(8상태)·대상자안내(4) — **누적상금내역 15화면 전부 포함**
- 뎁스 간 커넥터: 홈 카드→종목선택 · 홈 전체랭킹행→전체랭킹 · 홈 arrow→내상금내역(인터밴드 갭 레인) · 계좌미등록→계좌등록 · 내상금내역→대상자안내. (종목선택→주가예측·결과확인은 같은 뎁스라 생략, 홈→안내는 네이티브 트리거 없어 생략)

### 작업 순서 가드 (SSOT 16097:3930 §6 체크리스트에 박음)
- **0단계 트리거 맵**: 소스 화면별 인터랙티브 요소(bbox)→도착 매핑(네이티브 커넥터 endpoint 우선) → 뎁스·커넥터 출발점 판단의 입력. **빌드 전에** 수행
- **0단계 → 행 배치 도출 (「다른 화면=분기행」 누락 방지)**: 트리거맵에서 **한 출발화면이 *다른 트리거*로 *다른 도착화면* N개로 분기하면 → 분기행 N개**. 같은 화면의 다른 스테이트(조건·값만 차이)만 같은 행. ⚠️ 「설명/상세(무료)」를 "무료 유저 경로"라는 편의로 한 행에 묶은 게 누락 사례 — 둘은 홈무료의 다른 트리거(chevron vs 카드)로 가는 **다른 화면**이라 분기행. (블룸버그 v2, 2026-06-26)
- **4·5·6단계 §2 포인터**: 화면/라벨 y는 §2 수직공식으로 계산(즉흥 상수 금지). 공식은 §2에만, 체크리스트엔 링크만(SSOT 유지)
- **9단계 수치 자기검증**: 각 행 `screen_y == 이전행 bottom + 837` · `DEPTH_GAP == 800` · `도착 y == 도착화면 center` 코드 assert (**시각검증 前**)
- **9단계 → 행 배치 의미 검증**: 각 도착화면이 **트리거맵이 도출한 분기행**에 실제로 있는지 대조. ⚠️ 행간격·도착center·꺾임 수치검증은 통과해도 **「행 배치 *의미*」 오류는 못 잡는다** — 어제 트리거 *의미* 오류(수치는 ok였으나 트리거가 틀림)와 **같은 뿌리**. 수치검증 ≠ 의미검증.
- **섹션 위치(1단계)**: 신규 섹션 = 직전(최근) 섹션 우측 + **500px**, top y 동일. 예) 챌린지 = 내정보(우측 45836) + 500 → x46336, top -272
- **교훈**: 규칙이 가이드에 있어도 **'작업 순서'에 박혀있지 않으면 누락된다** → 포인터(빌드 중 적용) + 수치검증(빌드 후 탐지). 트리거 매칭·행간격 누락 둘 다 이 뿌리.

---

## 목표

Figma의 **Task 페이지**에 산재한 화면(Frame)들을 **Master 페이지**의 플로우 다이어그램 섹션으로 자동 이동·정렬한다.

수작업으로 하면 화면 하나씩 복사→붙여넣기→위치 조정→커넥터 그리기 반복이지만, Claude + Figma MCP Plugin API를 활용하면 이 과정을 스크립트로 처리할 수 있다.

### 최종 목표 상태

```
Master 페이지
└── "V.AI 개선 플로우" SectionNode
      ├── bg Rectangle (#BFBFBF 배경)
      ├── Depth 밴드 (D1 ~ D5, #3A3A3A, h=130)
      ├── 서브레이블 / 상태레이블 텍스트
      ├── 화면 22개 (Task 페이지에서 복제)
      └── 커넥터 벡터 21개
```

---

## 배경 및 대화 맥락

### 왜 자동화가 필요했나

Vestway V.AI 기능 개선을 진행하면서 Task 페이지에 30개 이상의 화면이 쌓였다. Master 페이지는 전체 앱 플로우를 한눈에 보는 다이어그램 페이지인데, Task에서 완성된 화면을 Master에 수동으로 옮기는 작업이 반복됐다.

- 화면 복제 → 위치 맞추기 → 밴드·레이블 추가 → 커넥터 그리기
- 화면이 많아질수록 수작업 실수 증가 (위치 오차, 커넥터 겹침)

### Claude와 함께 찾은 접근법

1. **Figma MCP를 통한 Plugin API 직접 제어** — Claude가 `use_figma` 도구로 JavaScript를 Figma 플러그인 컨텍스트에서 실행
2. **기준 섹션 역공학** — 이미 완성된 "블룸버그 리서치 조회" 섹션의 좌표·스타일을 분석해 레이아웃 상수 추출
3. **스크립트 패턴화** — 재사용 가능한 함수(mkVec, appendScreen 등)로 구조화

---

## 완료된 작업 (2026-06-09)

### V.AI 개선 플로우 섹션 생성

- **파일**: 테스트 파일 `TCFQPUYWPmBmwcpo7cCu8w`
- **Master 페이지 노드**: `14753:31917` ("🚧 Master")
- **생성된 섹션**: `16073:3930` ("V.AI 개선 플로우")
  - 위치: x=4700, y=4592 (기존 가이드 섹션 우측)
  - 크기: 6570×5748px

#### 배치된 화면 22개

| 화면 이름 | Depth | Row | 섹션 기준 x | 섹션 기준 y |
|-----------|-------|-----|------------|------------|
| vai/onboarding/1 | 1 | 0 | 360 | 1306 |
| vai/onboarding/2 | 1 | 0 | 755 | 1306 |
| vai/tutorial/1 | 2 | 0 | 1630 | 1306 |
| vai/tutorial/2 | 2 | 0 | 2025 | 1306 |
| vai/home/following | 3 | 0 | 2900 | 1306 |
| vai/home/find | 3 | 0 | 3295 | 1306 |
| vai/home/bloomberg-modal | 3 | 1 | 2900 | 2934 |
| vai/chat/1 | 4 | 0 | 4170 | 1306 |
| vai/chat/2 | 4 | 0 | 4565 | 1306 |
| vai/chat/3 | 4 | 1 | 4170 | 2934 |
| vai/chat/4 | 4 | 1 | 4565 | 2934 |
| vai/chat-source/2 | 4 | 1 | 4960 | 2934 |
| vai/chat-source/1 | 5 | 0 | 5440 | 1306 |
| vai/chat-copy/1 | 5 | 0 | 5835 | 1306 |
| vai/chat-bookmark/1 | 5 | 1 | 5440 | 2934 |
| vai/chat-report/1 | 5 | 1 | 5835 | 2934 |
| vai/chat-report/2 | 5 | 2 | 5835 | 4562 |
| vai/search/default | 4 | 2 | 4170 | 4562 |
| vai/search/focused | 4 | 2 | 4565 | 4562 |
| vai/search/manage | 4 | 2 | 4960 | 4562 |
| vai/search/result | 4 | 2 | 5355 | 4562 |
| vai/search/no-result | 4 | 2 | 5750 | 4562 |

#### 추가된 커넥터 21개

주요 연결 흐름:
- onboarding/1 → onboarding/2 → tutorial/1 → tutorial/2 → home/following
- home/following → chat/1 (팔로잉탭 AI 카드 클릭)
- home/find → chat/1
- home/following → bloomberg-modal (조건부: 리서치 탭 미방문 유저)
- home/following → search (+ 아이콘)
- chat/1 → chat/3 (이용약관), chat/4 (구독 유도)
- chat/2 → chat-source/1, chat-copy/1, chat-bookmark/1, chat-report/1
- chat-report/1 → chat-report/2

---

## 레이아웃 규칙 (추출된 상수)

기존 "블룸버그 리서치 조회" 섹션을 역공학해 추출한 레이아웃 상수.

### 수직 상수 (섹션 기준 y)

```
SECTION_MARGIN_TOP  = 360   ← 밴드 상단
BAND_H              = 130   ← 밴드 높이 (fill #3A3A3A)
BAND_BOTTOM         = 490

GAP_BAND_TO_SUBLBL  = 400
SUBLBL_Y (row0)     = 890
SUBLBL_H            = 58    ← 48px Bold 폰트 기준

GAP_SUBLBL_TO_STATE = 165
STATELBL_Y (row0)   = 1113
STATELBL_H          = 44    ← 36px Bold 폰트 기준

GAP_STATE_TO_SCREEN = 170
SCREEN_Y (row0)     = 1327
```

행 간격: 이전 행 화면들의 bottom에서 400px 아래가 다음 행 서브레이블 y.

### 수평 상수 — ⚠️ 구버전 (현행: `INTERNAL_GAP=60`, 밴드폭 `max(435N−60,810)` — 상단 현행 모델 참조)

```
SECTION_LEFT_MARGIN = 360   ← 첫 번째 열 시작 x
SCREEN_WIDTH (SW)   = 375   ← iPhone 표준
INTERNAL_GAP (SG)   = 40    ← (구버전 값. v2 실측 20, v3부터 60)
DEPTH_GAP (DG)      = 500   ← Depth 열 사이 간격

dx(Depth i) = 360 + i × 1270   ← (5-Depth 고정 가정, v3에서 폐기)
→ [360, 1630, 2900, 4170, 5440]
```

---

## 행 폭 한계 규칙 (2026-06-19, v2 시절) — ⚠️ 구버전 (현행 모델로 대체됨)

> **❗ 이 규칙은 v3에서 폐기.** v3는 네이밍=뎁스 모델이라 "한 행 최대 3화면" 제한이 없고
> (모달/액션 7화면이 한 행), 커넥터도 뎁스 간만 존재한다. 아래는 v2 시절 기록.

> **배경**: v2 섹션(16210:11148)에서 D4 검색 화면 5개를 한 행에 나열했더니
> 폭이 D5 컬럼을 침범했다. 표준 밴드폭(770)이 콘텐츠를 못 덮어
> "헤더는 짧은데 화면은 옆 Depth로 흘러가는" 오류가 발생.
> 아래 3규칙은 이 충돌을 **사전 차단**하기 위한 것.

### ① 한 Depth row 최대 화면 수 = 3개

화면 간 수평 step = **395px** (SW 375 + 내부 gap 20). ← 실측값. (구문서의 SG=40·step415는 오류)
n개 화면의 행 폭 = `395 × (n−1) + 375 = 395n − 20`.
검증: 2화면=770(=표준 밴드폭), 5화면=1955(=band-d4 실제폭). ✓

```
395n − 20 ≤ DEPTH_GAP(1270)  →  n ≤ 3.27
→ 한 row당 최대 3화면. 4개 이상이면 반드시 ③로 처리.
```

### ② 밴드(헤더) 폭 = 콘텐츠 기준 동적 산출

```
밴드 폭 = max(해당 Depth 가장 넓은 row의 화면 폭, 770)
       = max(395·n_max − 20, 770)
```

- **고정 770 금지.** 밴드를 콘텐츠 폭으로 그리면 침범이 **밴드끼리 겹쳐 즉시 보임** → 밴드 자체가 오류 감지기.
- 예) D4 검색 5개 → 밴드 폭 = `395×5 − 20 = 1955`.

### ③ 행 폭 초과(4화면+) 처리 정책

한 row가 3화면을 넘으면 둘 중 하나로 처리하고 **반드시 가이드/로그에 명시**:

- **(A) row 분할** — 3개씩 줄바꿈해 세로로 쌓기. 레이아웃 깔끔, **기본 추천**.
- **(B) 열 확장** — 해당 Depth 폭을 넓히고 **이후 모든 Depth를 우측 shift**.
  한 줄로 꼭 보여줘야 할 때만.
  ```
  shift = (초과 Depth의 최대 row 폭) − 770
  → 이후 Depth 전체(밴드·서브/상태레이블·화면·도착 커넥터)를 +shift
  ```
  - 예) D4 검색 5개: `1955 − 770 = +1185` → D5 전체 우측 이동.
  - 커넥터: **출발(고정) vertex만 남기고 도착측 vertex 전부 +shift** 하면 형태 보존.
  - 새 갭 = (넓힌 밴드 끝) → (다음 밴드 시작) = 표준 500 유지.

### 검증 패스 (배치 후 필수)

각 화면 `[x, x+SW]` 가 **다음 Depth 밴드 시작 x** 를 넘는지 자동 체크.
넘으면 침범 경고 → ② 밴드 폭 재산출 + ③ 처리. (현재 누락돼 있던 단계)

---

## 핵심 코드 패턴

### 커넥터 생성 함수 (mkVec)

```javascript
function mkVec(pts, name, sec) {
  var xs = pts.map(function(p){return p[0];});
  var ys = pts.map(function(p){return p[1];});
  var ox = Math.min.apply(null, xs), oy = Math.min.apply(null, ys);
  var v = figma.createVector();
  v.vectorNetwork = {
    vertices: pts.map(function(p){return {x: p[0]-ox, y: p[1]-oy};}),
    segments: pts.slice(0,-1).map(function(_,i){return {start:i, end:i+1};}),
    regions: []
  };
  v.x = ox; v.y = oy;
  v.fills = [];
  v.strokes = [{type:'SOLID', color:{r:0.478,g:0.478,b:0.478}}];
  v.strokeWeight = 2;
  v.strokeCap = 'ARROW_EQUILATERAL'; // 도착 vertex에만 적용
  v.name = 'conn:' + name;
  sec.appendChild(v);
  return v;
}
```

Z-shape 커넥터 좌표 패턴:
```javascript
// midX = 두 화면 사이 갭의 수직 구간 x
[[dep[0], dep[1]], [midX, dep[1]], [midX, arr[1]], [arr[0], arr[1]]]
```

### Task → Master 화면 복제

```javascript
await figma.setCurrentPageAsync(taskPage);
const src = taskPage.findAll(n => n.name === 'vai/screen/name')[0];

await figma.setCurrentPageAsync(masterPage);
const clone = src.clone();
sec.appendChild(clone);
clone.x = target_x;  // 섹션 기준 상대 좌표
clone.y = target_y;
```

---

## 막힌 지점 (Blockers)

### 1. Figma Section 내 노드 접근 불가

**현상**: `figma.getNodeById('sectionsNodeId')` → null 반환  
**원인**: Figma Plugin API에서 비현재 페이지의 SectionNode는 children이 빈 배열로 반환됨  
**해결**: `await figma.setCurrentPageAsync(targetPage)` 로 페이지 먼저 전환 후 접근

```javascript
// 잘못된 방법
const sec = figma.getNodeById('16073:3930'); // null

// 올바른 방법
await figma.setCurrentPageAsync(masterPage);
const sec = figma.getNodeById('16073:3930'); // 정상 접근
```

### 2. createConnector() 미지원

**현상**: `figma.createConnector()` → 함수 없음 오류  
**원인**: Figma Plugin API에 커넥터 생성 함수가 없음  
**해결**: `figma.createVector()` + `vectorNetwork` 수동 설정으로 커넥터와 동일한 벡터 생성

### 3. vectorNetwork 누적 오프셋 버그

**현상**: 기존 커넥터를 업데이트할 때 좌표가 이상하게 이동  
**원인**: `vectorNetwork` 재설정 시 기존 `v.x`, `v.y` 오프셋에 새 좌표가 누적됨  
**해결**: 업데이트 전 반드시 `v.x = 0; v.y = 0;` 리셋 필요

### 4. Cross-page 화면 복제 좌표 혼동

**현상**: Task 페이지에서 clone한 화면을 Master 섹션에 appendした 후 좌표가 맞지 않음  
**원인**: `appendChild` 전후로 좌표 기준이 page-absolute → section-relative로 바뀜  
**해결**: `sec.appendChild(clone)` 직후에 섹션 기준 상대 좌표로 x/y 설정

### 5. 커넥터 겹침 문제

**현상**: 동일 Depth 갭을 통과하는 커넥터가 겹쳐서 어떤 연결인지 불명확  
**해결**: 갭을 N등분해 midX 분리
- 커넥터 2개: 갭 내 1/3 지점, 2/3 지점
- 커넥터 3개: 1/4, 1/2, 3/4 지점
- 타겟이 먼 커넥터 → midX를 갭 좌측에 배치

### 7. D4 검색 행이 D5 컬럼 침범 (행 폭 초과) — 2026-06-19

**현상**: v2 섹션(16210:11148) D4 row2에 검색 화면 5개를 한 줄 배치 →
폭 1955px(4170~6125)가 D5 밴드(5440)를 깊숙이 침범. chat-report/2는 땜질로 6175까지 도망쳐 있었음.
**원인**: 한 row 3화면 한계(`행 폭 한계 규칙 ①`) 초과 + 밴드폭 770 고정으로 콘텐츠 미커버.
**해결 (B안)**: → `행 폭 한계 규칙 ③(B)` 적용.
- D5 화면 4개(source-1·copy-1·bookmark·report-1) 전부 **+1185** 이동 (band-d5/D5 레이블 포함)
- chat-report/2 땜질(6175) 해제 → 정규 그리드 복귀(7020, report/1 바로 아래) → 커넥터 직선화
- band-d4 폭 770 → **1955** (검색 5개 커버)
- 섹션·bg 폭 6910 → **7755**
- D5향 커넥터 4개: 출발 vertex(4940,1702) 고정 + 도착측 +1185 → 형태 보존

### 6. 서브레이블 높이 불일치

**현상**: V.AI 섹션 화면 y가 기준 섹션(블룸버그)과 다름  
**원인**: V.AI 섹션 작업 시 서브레이블 `textAutoResize` 실행 전 높이를 36px로 잘못 사용 (정상: 58px)  
**영향**: 화면 y = 1306 (정상: 1327)  
**향후 처리**: 신규 섹션은 Bloomberg 기준(h=58, screen y=1327) 통일

---

## 현재 작업 상태 (2026-06-12 기준)

| 항목 | 상태 |
|------|------|
| V.AI 개선 플로우 섹션 생성 | ✅ 완료 |
| 화면 22개 배치 | ✅ 완료 |
| 커넥터 21개 추가 | ✅ 완료 |
| 레이아웃 상수 문서화 | ✅ 완료 |
| 자동화 스크립트 패키징 | ⬜ 미착수 |
| 신규 화면 추가 자동화 | ⬜ 미착수 |

---

## 다음 행동 (Next Actions)

### 즉시 가능한 작업

1. **신규 화면 추가 자동화**  
   Task 페이지에 새 화면이 생겼을 때 한 줄 요청으로 Master 섹션에 추가  
   ```
   "vai/home/new-feature 화면 D3 Row1에 추가해줘"
   ```

2. **서브레이블 높이 통일**  
   V.AI 섹션의 서브레이블 h를 36 → 58로 수정, 이에 따른 화면 y 1306 → 1327 일괄 조정

3. **스트로크 마커 활용**  
   위치가 바뀐 화면에 stroke를 추가해두면 Claude가 자동으로 필터링해 위치 재조정 가능

### 중장기 자동화 아이디어

4. **Task 페이지 스캔 → Master 자동 동기화**  
   Task 페이지의 모든 `vai/` 프리픽스 화면을 스캔해 Master에 없는 화면 감지 → 자동 추가

5. **플로우 정의 파일 기반 생성**  
   JSON으로 플로우 정의 (`screens`, `connections`, `layout`)를 작성하면 Claude가 Figma에 자동 생성

   ```json
   {
     "section": "V.AI 개선 플로우",
     "screens": [
       {"name": "vai/onboarding/1", "depth": 1, "row": 0},
       ...
     ],
     "connections": [
       {"from": "vai/onboarding/1", "to": "vai/onboarding/2", "trigger": "CTA 버튼"}
     ]
   }
   ```

---

## Claude에게 요청하는 방법 (다른 디자이너를 위한 가이드)

### 기본 패턴

```
"[화면명] 화면을 Master 페이지 V.AI 섹션 D[N] Row[N]에 추가해줘"
```

### 커넥터 추가

```
"vai/home/following → vai/chat/1 커넥터 D3→D4 갭에 추가해줘.
 트리거: 홈 화면 팔로잉탭 AI 카드 클릭 (y=450)"
```

### 전체 섹션 새로 생성

```
"Task 페이지의 vai/ 화면 목록 전체 스캔해서
 Master 페이지에 새 섹션 '[섹션명]' 만들고 레이아웃 상수 기준으로 배치해줘"
```

### 현재 섹션 상태 확인

```
"Master 페이지 V.AI 섹션 현재 상태 확인해줘 — 화면 수, 커넥터 수, 빠진 화면 있는지"
```

---

## 사용 도구 스택

| 도구 | 용도 |
|------|------|
| **Claude (claude-sonnet-4)** | 전체 자동화 오케스트레이션 |
| **Figma MCP** | Figma Plugin API 직접 제어 |
| **Figma Plugin API** | SectionNode 생성, clone, vectorNetwork |

---

## Figma 파일 정보

| 항목 | 값 |
|------|-----|
| 테스트 파일 키 | `TCFQPUYWPmBmwcpo7cCu8w` |
| Master 페이지 노드 | `14753:31917` |
| **V.AI 개선 플로우 v3 (현행)** | **`16245:20440`** — 네이밍=뎁스, gap60, 커넥터 뎁스간만 (2026-06-22) |
| 소스: V.ai 개선 (V.AI 페이지) | `16189:41312` — v3 화면 클론 출처 |
| V.AI 개선 플로우 v2 (구버전) | `16210:11148` — 5-Depth, 행폭 규칙 시절 |
| V.AI 개선 플로우 (구버전) | `16185:3930` / 최초 폐기 `16073:3930` |
| 기준 섹션 (블룸버그) | `16037:31196` |
| **Figma 내부 가이드 문서 (SSOT)** | `16097:3930` ("📐 플로우 섹션 생성 가이드", Master 페이지) |

> **가이드 SSOT = Figma 섹션 `16097:3930`** (2026-06-19 운영방식, 2026-06-22 스냅샷): 작업 중 규칙 변경은
> **Figma 문서에만** 반영(전부 Inter 폰트 → `characters` 직접 수정 가능). **이 `guide.md`는
> 마일스톤(규칙 확정) 시 받는 스냅샷** — 매 변경마다 동기화하지 않으므로, 최신 규칙은
> 항상 Figma 문서를 우선 확인할 것.

> **v2 좌표 주의**: v2는 서브레이블 h=58 통일로 화면 y가 구버전과 다름 —
> row0=1327 / row1=3561 / row2=5224. D4 검색행 후 D5는 +1185 shift됨(밴드/레이블/화면).
> band-d4 폭=1955, band-d5 x=6625.
