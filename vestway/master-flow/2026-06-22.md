# 작업 로그 — 2026년 6월 22일 (v3 + 모델 재정립 마일스톤)

> 작성자: 태휘 · 앱: Vestway · 테스트 파일 `TCFQPUYWPmBmwcpo7cCu8w`
> 여러 날(06-18~22)에 걸친 v3 작업·규칙 재정립을 마일스톤으로 정리.

---

## 큰 그림
v2(5-Depth, 행 폭 한계 규칙)에서 → **네이밍 기준 뎁스 모델**로 재정립하고, 소스에서 새로 만든 **v3 섹션**에 적용. 규칙은 Figma SSOT(16097:3930)에 반영, git `guide.md`에 스냅샷.

## v3 섹션 (`16245:20440`, Master 페이지)
- 소스 `V.ai 개선`(`16189:41312`, V.AI 페이지)에서 화면 21개 새로 클론 (v2 복제 아님 — 최신 네이밍 반영)
- 구조: **D1 온보딩 / D2 튜토리얼 / D3 홈(+블룸버그 모달 행) / D4 home하위 전체**
  - D4: 대화하기(대화·입력·추천질문) / 모달·액션(이용약관·구독·출처·복사·북마크·신고·신고접수 7화면) / 검색하기 5화면
- 서브·상태레이블, 뎁스 간 커넥터 4개

## 확정된 규칙 (→ SSOT + git 반영)
1. **네이밍 `/` = 뎁스**. `vai/home/*` 전부 같은 뎁스(D4). 모달·같은화면 동작은 페이지 이동 아님 → 뎁스 안 늘어남. 모달은 그 화면과 같은 뎁스(블룸버그=D3, chat모달=D4).
2. **행 배치**: chat 본체·상태 같은 행 / 모달 별도 행 / 한 행 화면수 제한 없음.
3. **수평**: `INTERNAL_GAP 20→60` (step 435), 밴드폭 `max(435N−60, 810)`.
4. **커넥터는 뎁스 간 이동만 표기.** 같은 뎁스 내(같은 행·다른 행) 연결 안 함. 조건레이블(다이아몬드·4회미만/이상) 표기 안 함.
5. **`firstBend ≥ screen_right+10` 삭제** (뎁스 간만이라 공간 충분).
6. **트리거 인식**: 커넥터 실제 **bbox 시작점** → 그 점을 **포함(거리0)/최근접**하는 인터랙티브 컴포넌트 = 트리거. **magnet(AUTO) 추정 금지**(타겟 방향 가장자리로 빠져 빗나감). 후보=INSTANCE/COMPONENT·button/cta/icon.

## 트리거 인식 검증 (소스 커넥터 점검)
- home→search: 실제 bbox 시작점(3120,1462) → `plus_circle`(+아이콘) ~11px = 정답 ✅
- home→chat: 새 커넥터(`16287:73101`) 시작점(3116,1858)이 첫 `feed-cta` bbox 안 = 트리거=feed-cta ✅
- home→bloomberg: 하단중앙(BOTTOM magnet)서 시작 → 최근접이 하단 카드/네비로 애매. 트리거 카드서 다시 그려야 정확.
- **교훈**: magnet 데이터로 추정하면 빗나감(home→search를 AUTO→BOTTOM으로 오판). 실제 bbox 시작점을 봐야 함.

## 부산물
- **Flow Connector 플러그인** 작성: `~/figma-flow-connector/` (선택 2개 → `conn: 출발 > 도착` 자동 네이밍, 네이티브 우선/벡터 폴백). 단, 사용감 별로라는 피드백 → 이름 규칙/트리거 인식 쪽으로 선회.

## 기록 갱신
- Figma SSOT `16097:3930`: 섹션4(뎁스=네이밍·gap60), 섹션5(뎁스간 커넥터 규칙·트리거 인식 규칙), firstBend 삭제
- git `guide.md`: 상단에 "현행 모델(v3)" 섹션 추가, 옛 섹션(5-Depth·행폭 규칙·SG40) 구버전 표시, 파일정보 v3로 갱신

## 내정보 플로우 (Master 페이지) — v3 모델 적용 테스트
소스: 내정보 페이지 `9123:23931`("Flow" 영역). 사용자가 네이밍을 `/`=뎁스 · `-`=상태로 정리.
새 섹션 **`16297:15585`** ("내정보 플로우", Master, v3 아래)에 화면 19개 클론·배치.
- **뎁스**: D1 `my`(free·free trial) / D2 구독하기·이용권내역(정기구독중·해지신청·해지완료)·내정보수정·언어설정·알림설정 / D3 약관(selling/notice)·구독해지(cancel +확인모달2·alert)·수정항목(닉네임·비번·번호·인증·완료)
- **커넥터**: my→5메뉴, selling→약관, 이용권내역→해지, 내정보수정→닉네임, 확인모달→해지완료

### 신규 규칙 (이 작업에서 도출 → SSOT 반영)
1. **확인 모달 규칙**: 확인 모달(Basic dialog)은 **부모 화면과 같은 뎁스(모달 행)** 에 배치, 부모↔모달 커넥터 없음(나열), **"확인의 행선지(결과 화면)"만** 커넥터. (가이드 텍스트 정식 추가 여부는 미정 — 현재 섹션에 적용만)
2. **자동 트리거 매칭**: 커넥터 새로 그릴 때 → 소스 화면 안 **행/요소(메뉴행·버튼·카드) 텍스트·이름 스캔** → 목적지와 **의미 매칭** → 그 요소 우측/행 y에서 출발. 예) "이용권 내역" 카드→subscription, "내 정보 수정하기"→edit, 언어/알림 설정, my-free "2주 무료 체험 시작하기"→selling. **→ SSOT 섹션5 [자동 트리거 매칭] 노트 추가됨** (cascade 136px).

## 다음
- 내정보 플로우: edit 하위 추가 커넥터(비번·번호), 확인 모달 2개 행선지 구분
- **확인 모달 규칙**을 가이드 텍스트로 정식 추가할지 결정
- git `guide.md`·메모리에 확인모달/자동트리거 매칭 마일스톤 스냅샷 (현재 Figma SSOT만)
- home→bloomberg 트리거 정밀화 / v2·구버전 섹션 정리 / 상태레이블 검수
