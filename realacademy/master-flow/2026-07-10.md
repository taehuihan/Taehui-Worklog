# 2026-07-10 — comp-guide 신설 + 학습커스텀 -COMP 규칙대로 재정리

> master-flow 요청 시 그 플로우의 마스터 컴포넌트도 함께 정리하는 워크플로 도입. 규칙 문서(`comp-guide.md`)를 §1~§4로 확정하고, 리아 학습커스텀 -COMP를 그 규칙대로 재빌드.

## comp-guide.md (신설, `_shared/master-flow/`)
- §1 스타일: bg·마진(360)=master-flow §4-1 동일 / width=위 플로우와 동일(초과불가·wrap) / 위치=플로우 하단 +500
- §2 대상: **소스 페이지 안의 마스터만** (공유·중앙 페이지 제외)
- §3 이동·배치: 소스 마스터 이동 / **큰 요소(합성)=우측·포함된 작은 요소=좌측** / 큰 요소 단위 wrap / **이름 라벨 미표기**
- §4 검증: 수치(width·위치·마진·겹침·누락) + 의미(소스페이지 소속·인스턴스 무결성) + 시각(배치·wrap·라벨)

## 학습커스텀 -COMP 재정리 (`50809:12120`)
- **대상 6개** = 소스 페이지(`📍 추천학습 커스텀`)의 마스터: **홈/커스텀**(완성형) + custom-learning card · Frame 2147236912 · Component 506 · Component 504 · button-drag(부품). ⚠️ 이전 실험의 공유 마스터 인스턴스 11개는 §2대로 **전부 제외**(home·coin·learning card 등은 common comp·today's recommend comp 등 소스 페이지 밖).
- **이동**: 소스 페이지 마스터 5개를 -COMP로 이동(홈/커스텀은 이미 이동돼 있었음).
- **배치 (§3 = 포함 계층 좌→우 트리)**: 크기 기준 = 쪼개지는 단위(포함 계층). **큰(합성)=좌측 → 작은=우측**, **각 마스터의 하위 부품은 그 마스터 바로 옆(우측·같은 행)** = 부모별 서브트리. 홈/커스텀(좌) → 직속 형제 custom-learning card·Frame 2147236912(행으로 쌓음) → card 옆 button-drag / **Frame 옆 Component 506→504**. margin 360, 라벨 없음.
- **섹션**: x−129, y7141(=플로우 bottom 6641 +500), width **10920**(=플로우 동일), h1563, bg #DCDCDC.
- ⚠️ §3 규칙 2회 교정: ① "작은→큰·큰=우측·단일행" → ② "큰→작은·큰=좌측·같은 tier 행" → ③ **"부모별 서브트리(자식은 부모 옆, 전역 tier 열 ❌)"** 로 최종 확정.

## 검증 (전항 통과)
- 수치: width==플로우 / top==flow bottom+500 / x정렬 / 마진360 / 컴포넌트 겹침0 / 대상 누락0
- 의미: 6개 전부 소스 페이지 소속 · 플로우 인스턴스 **1615개 검사 brokenMainComp 0** · 커스텀 화면 렌더 정상
- 시각: 큰=우측/작은=좌측 · 이름 라벨 없음 · bg 확인

## TODO (사용자 결정 시)
- ~~`/master-flow-new` 스킬 워크플로에 "빌드 후 comp-guide.md대로 -COMP 정리" 단계 추가~~ → ✅ **연결 완료 (07-10, 아래)**
- 배치 정렬(좌정렬 flow vs 우정렬)·wrap 다행 케이스는 다른 플로우에서 재확인.

## `/master-flow-new` ↔ comp-guide 연결 (07-10, 태휘 지시)
- **SKILL.md** (`Taehui-Worklog/.claude/skills/master-flow-new/SKILL.md`): 새 섹션 「컴포넌트 정리 (-COMP)」 **9단계** 추가 — 플로우 검증(7·8) 통과 후 소스 페이지에 로컬 마스터 있으면 comp-guide.md **직접 Read**(기억 복원 ❌) 후 -COMP 생성 + 3중 검증(수치·의미·시각). 없으면 skip + 로그에 "대상 마스터 0" 명기. 워크로그 기록은 10단계로 밀고 -COMP 결과 항목 추가.
- **comp-guide.md**: WIP 헤더 → "✅ 스킬 연결됨 (2026-07-10)"으로 갱신. 규칙 개정은 이 문서만 고치면 됨 (SSOT).
- **개인 포인터 커맨드 2개** (`~/.claude/commands/master-flow-{new,update}.md`): SSOT 경로 구버전 `vestway/master-flow/` → `_shared/master-flow/` 수정 + comp-guide.md 추가 (07-08 이관 미반영이었음).
- ⚠️ `/master-flow-update` SKILL.md에는 comp 단계 미연결 (업데이트 시 마스터 추가/삭제 반영 규칙은 별도 결정 필요).

## 디자이너 공유 문서 작성 (07-10)
- **`_shared/master-flow/share/2026-07-10_flow-step.md`** — Depth→Flow Step 변경의 문제(전진 모달 커넥터 단절 + 자동 상태전환 오배치, 공통 뿌리="화면 전환≠플로우 전진")와 해결(Flow Step 3분기 판정표 + 슬래시/하이픈 네이밍 컨벤션). 예시는 화면명 없이 일반화(초기화 확인 모달·홈 A형/B형). 부록: 모양 마커·이탈 코리도·comp-guide 요약.

## 레벨테스트 플로우 신규 생성 (`/master-flow-new`, 조건 분기 규칙 첫 적용)

> 소스 `43790:37933` "TOEFL Primary 기반 레벨테스트 연동" (페이지 `📍 레벨테스트`) → Master `48227:364890`

### 신규 규칙 §2-4 조건 분기 행 (태휘 결정, 이 세션에서 확정)
- **문제**: 기존 §5 분기행은 *다른 트리거 → 다른 화면*만 다룸. **같은 트리거 → 조건에 따라 A/B**(소스 ◇ 다이아)는 §2-3이 "Master 미표기"라 **분기 정보가 통째로 사라지고 있었음.**
- **확정**: ① 분기 화면을 **같은 열의 다른 행**으로 분리(열 ❌ — 유저는 한 번 눌렀고 단계는 한 칸) ② **행 순서 = 소스 화면 행 순서**(트리거가 하나라 §2 트리거 위치 기준 입력 없음) ③ **조건은 서브레이블 괄호 병기**(`[콘텐츠명] (조건)`, 콘텐츠명 대체 ❌) ④ **커넥터 라벨 없음** ⑤ 다이아 도형 자체는 미표기.
- **세그먼트(⬜)와 구분**: 세그먼트=진입점·행그룹 통째 분리·커넥터 없음 / 조건분기=플로우 중간·공통 부모·부모→A·B 커넥터 있음.
- ⚠️ 미정: 분기 후 **merge** 케이스 (실사례 없음).

### 빌더 갭 수정 (`connect-builder.js`)
- **이탈 코리도가 조건 분기를 오작동**: 같은 트리거 팬아웃(x2·cy 동일)을 "같은-y 형제 버튼"으로 오인 → 커넥터 하나를 화면 위/아래로 불필요 우회.
- 수정: `crossesRight`에 **`o._trig.x2 > sp._trig.x2`** 조건 추가(동일 트리거 제외). 팬아웃 분산은 레인 분리로 충분. → 빌드 결과 `corridor:false` 6/6 확인.

### 0단계 트리거맵 (소스 네이티브 커넥터 13개 전수 실측)
| 소스 커넥터 | 트리거 | from → to | Master |
|---|---|---|---|
| `43953:514191`+`514804` | GV 라이팅 카드 [시작하기] | 홈 → GV레슨홈 | ✓ D1→D2 (중간 주석박스 미표기) |
| `43953:514871` | (자동 노출, 화면 통째) | GV레슨홈 → 레테응시모달 | ✓ D2→D3 |
| `50852:73641`+`73650(N)` | 모달 [응시하러 가기] | 레테응시모달 → 산타앱1 | ✓ D3→D4 **분기행 0** |
| `50852:73641`+`73655(Y)` | 모달 [응시하러 가기] | 레테응시모달 → 산타앱3 | ✓ D3→D4 **분기행 1** |
| `43796:16051` | 테스트 화면 | 산타앱2 → 결과 | ✓ D4→D5 |
| `43788:16001` | 결과 보기 버튼 화면 | 산타앱3 → 결과 | ✓ D4→D5 |
| `43730:27288` | 디스크립션 화면 | 산타앱1 → 산타앱2 | ✗ 같은 flow step §5 (상태레이블로 순서 표현) |
| `43860:19865` | 산타앱2 topbar X | 산타앱2 → 레슨종료확인모달 | ✗ 비전진 확인 모달 §9 → 모달행 나열 |
| `43853:19783` | 결과 [확인] | 결과 → "GV 홈"(라벨박스) | ✗ 역방향 복귀·도착이 화면 아님 |
| `50852:73624`/`73636` | — | ⬜ 세그먼트 마커 2개 | 서브레이블 괄호 병기 |

- **마커 시각 확인**: ◇ "응시 유무 확인"(x6100, 내부) = 화면 분기점 / ⬜ "앱 최초 진입 유저"(x444) · ⬜ "알림톡"(x444) = 유저 세그먼트 2개.
- **제외**: `img-motion-big-conffetti`(visible=false) · COMPONENT_SET 2개(variant 정의 → -COMP로) · 주석 프레임 4개(노출 조건 메모).

### 결과 — 섹션 `50858:12129` "레벨테스트 플로우"
- 위치 x21971 y−757 (칭찬저금통 right 21471 +500), **12080×7738**, bg/섹션 `#DCDCDC`
- **화면 10개**: D1 홈 / D2 GV레슨홈 / D3 레테응시모달 / D4 산타앱1·산타앱2(row0) · 산타앱3(row1 **조건분기**) · 레슨종료확인모달(row2 모달행) / D5 결과(1280×1054)
- **세그먼트 B(알림톡)**: row3에 결과(모바일웹) 360 + 결과(pc웹) 1280, 상태레이블 MO/PC, 커넥터 없음(§2-2)
- **밴드**: d1 x360 w1700(세그먼트B가 폭 결정) / d2 2860 / d3 4940 / d4 7020 w2620 / d5 10440
- **서브레이블 괄호 병기**: `리아 홈 (앱 최초 진입 유저)` · `레벨테스트 (응시 안 본 경우)` · `레벨테스트 (응시 완료한 경우)` · `레벨테스트 결과 (알림톡)`
- **커넥터 6개** — 전부 첫꺾임·도착중앙·관통 ok, `corridor:false`
- 모달 클론 rename: `…/산타앱-레슨 종료 확인 모달` (하이픈 = 같은-화면 오버레이 §1)

### 검증
- **수치**: 행간격 837×3(2381→3218, 4018→4855, 5109→5946, 전부 **글로벌 max bottom** 기준) / DEPTH_GAP 800×4 / 밴드 커버리지 5/5 / 섹션 gap 500 / 겹침 0
- **의미**: `verifyRowBranching` 위반 없음 · 트리거맵 ↔ 커넥터 1:1 · 소스 네이티브 출발점 대조 일치(모달 CTA 우측 = 소스 x5445,y1715.5와 동일)
- **시각**: 섹션 전경 + 홈 화면 확대 — `ambiguous` 였던 `시작하기`(×5 매칭)가 **첫 카드 "GV 라이팅"의 CTA 우측**을 정확히 잡은 것 육안 확인. 분기 2선이 모달 CTA 한 점에서 위/아래로 갈라짐 확인.
- ⚠️ **GV레슨홈→레테응시모달은 유저 트리거가 없다**(모달 자동 노출 — 소스 주석 "GV레슨 홈 첫 진입 시에만 노출"). 소스도 화면 통째에 커넥터를 붙였고, Master는 `bg`(전체폭) 우측 = **화면 우측 중앙**에서 출발시켜 소스와 동일하게 맞춤. 트리거 날조 아님. → **가이드에 "자동 노출 전진 모달" 출발점 규칙 명문화 필요**(미반영).

### -COMP `50863:16564` "레벨테스트 플로우-COMP"
- 대상 = 소스 페이지(`📍 레벨테스트`) 마스터 **2개**: `Frame 1000007141`(776×720, 3 variants) · `Component 3`(1028×483, 3 variants). 둘 다 결과 화면의 레벨 카드 variant 정의.
- 배치: 최상위 **형제**(포함관계 없음) → 같은 열(x360) **행으로 쌓기**(§3), 세로 gap 120 · 라벨 없음 · 마진 360 (기존 `50809:12120` 실측 규칙과 동일)
- 섹션 x21971 y7481(=플로우 bottom 6981 +500), **12080×2043**(width=플로우 동일), bg #DCDCDC
- **검증**: 수치(width==플로우 / top==+500 / x정렬 / 마진360 / 겹침0 / 누락0) · 의미(**인스턴스 580개 brokenMainComp 0**, 결과 화면 렌더 정상) · 시각(라벨 없음·배치·bg)
- ⚠️ 소스 부작용: 마스터가 빠진 자리에 `Tablet`/`WEB` **텍스트 라벨만 남음** → **07-11 규칙 추가로 해소**(아래).

## TODO — 다음주 (2026-07-13~, 태휘 예약)
- **`/master-flow-update` comp 반영 규칙 결정·연결**: 소스 변경(화면 추가·교체·삭제·rename·split) 시 기존 -COMP 섹션에 마스터 추가/삭제/재배치를 어떻게 반영할지 규칙 확정 → comp-guide.md에 §추가 → update SKILL.md에 단계 연결.
- **자동 노출 전진 모달 출발점 규칙** guide.md 명문화 (레벨테스트 GV레슨홈→레테응시모달 사례).
- **§2-4 merge 케이스** 규칙 (분기 후 수렴) — 실사례 나오면.
