# 2026-07-10 — comp-guide 신설 + 학습커스텀 -COMP 규칙대로 재정리

> master-flow 요청 시 그 플로우의 마스터 컴포넌트도 함께 정리하는 워크플로 도입. 규칙 문서(`comp-guide.md`)를 §1~§4로 확정하고, 리아 학습커스텀 -COMP를 그 규칙대로 재빌드.

## comp-guide.md (신설, `_shared/master-flow/`)
- §1 스타일: bg·마진(360)=master-flow §4-1 동일 / width=위 플로우와 동일(초과불가·wrap) / 위치=플로우 하단 +500
- §2 대상: **소스 페이지 안의 마스터만** (공유·중앙 페이지 제외)
- §3 이동·배치: 소스 마스터 이동 / **큰 요소(합성)=우측·포함된 작은 요소=좌측** / 큰 요소 단위 wrap / **이름 라벨 미표기**
- §4 검증: 수치(width·위치·마진·겹침·누락) + 의미(소스페이지 소속·인스턴스 무결성) + 시각(배치·wrap·라벨)

## 학습커스텀 -COMP 재정리 (`50809:12120`)
- **대상 6개** = 소스 페이지(`📍 추천학습 커스텀`)의 마스터: **홈/커스텀**(완성형) + custom-learning card · Frame 2147236912 · Component 506 · Component 504 · button-drag(부품). ⚠️ 이전 실험의 공유 마스터 인스턴스 11개는 §2대로 **전부 제외**(home·coin·learning card 등은 common comp·today's recommend comp 등 소스 페이지 밖).
- **이동**: 소스 페이지 마스터 5개를 -COMP로 이동(홈/커스텀은 이미 이동돼 있었음).
- **배치 (§3 = 포함 계층 좌→우 트리)**: 크기 기준 = 쪼개지는 단위(포함 계층). **큰(합성)=좌측 → 작은=우측**, **각 마스터의 하위 부품은 그 마스터 바로 옆(우측·같은 행)** = 부모별 서브트리. 홈/커스텀(좌) → 직속 형제 custom-learning card·Frame 2147236912(행으로 쌓음) → card 옆 button-drag / **Frame 옆 Component 506→504**. margin 360, 라벨 없음.
- **섹션**: x−129, y7141(=플로우 bottom 6641 +500), width **10920**(=플로우 동일), h1563, bg #DCDCDC.
- ⚠️ §3 규칙 2회 교정: ① "작은→큰·큰=우측·단일행" → ② "큰→작은·큰=좌측·같은 tier 행" → ③ **"부모별 서브트리(자식은 부모 옆, 전역 tier 열 ❌)"** 로 최종 확정.

## 검증 (전항 통과)
- 수치: width==플로우 / top==flow bottom+500 / x정렬 / 마진360 / 컴포넌트 겹침0 / 대상 누락0
- 의미: 6개 전부 소스 페이지 소속 · 플로우 인스턴스 **1615개 검사 brokenMainComp 0** · 커스텀 화면 렌더 정상
- 시각: 큰=우측/작은=좌측 · 이름 라벨 없음 · bg 확인

## TODO (사용자 결정 시)
- `/master-flow-new` 스킬 워크플로에 "빌드 후 comp-guide.md대로 -COMP 정리" 단계 추가 (아직 미연결).
- 배치 정렬(좌정렬 flow vs 우정렬)·wrap 다행 케이스는 다른 플로우에서 재확인.
