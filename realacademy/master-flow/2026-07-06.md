# 2026-07-06 — 학습 커스텀 플로우 Master 섹션 신규 생성

> 파일: `MOJH4fo6X8hy7tfr3Spxuv` (🔍 RA- PAGE UI_All) — **테스트 파일 밖 첫 Master 플로우 작업 (사용자 명시 지정)**
> 워크플로: VestWay `vestway/master-flow/guide.md` + SSOT(테스트파일 `16097:3930`) 그대로 적용
> 스킬: /master-flow-new

## 결과

| 항목 | 값 |
|------|-----|
| 소스 섹션 | `47909:2828` "학습 커스텀" |
| 타겟 페이지 | `48227:364890` "🚧 [New] Master" |
| 생성 섹션 | **`50686:3312` "학습 커스텀 플로우"** — x−129 y−757, 10920×5761 (Home-COMP 우측+500, top 동일) |
| 화면 | 9개 클론 (전부 1280×800 태블릿) |
| 커넥터 | 3개 |
| 레이블 | 서브 7 + 상태 4 (Inter Bold 48/36, h58/44 ✓) |

## ⚠️ 편차 (VestWay 가이드 대비)

1. **태블릿 화면폭 1280** (모바일 375 아님) — gap 상수는 SSOT 유지: INTERNAL_GAP 60 → step **1340**, DEPTH_GAP 800, 밴드폭 = 1340×N−60. 밴드 x = 360 / 2440 / 5860 / 9280.
2. **세그먼트 = 신규 유저 / 기존 유저** (구매 무료/유료 아님). 다이아 표기: D1 좌측(x558)=세그먼트 2개, 내부(x4113) "무료 체험 5일째 날"=화면분기(시간 조건 → 커넥터 날조 금지로 생략).

## 구조

- **Row0 (신규 유저)**: D1 홈(전체노출형) / D2 홈_추천형[튜토리얼·디폴트] / D3 커스텀 화면[디폴트·드래그나 학습 추가or제거 시] / D4 홈(커스텀형)-설정완료 — y1327
- **Row1**: D3 초기화 확인 모달(모달행) / D4 홈(추천형)-설정완료 — y2964
- **Row2 (기존 유저)**: D1 공지 모달(tablet-dim) — y4601
- 제외(사용자 확인): contents 520×992·Section 1 540×806 = 공지 모달 콘텐츠 참고자료

## 트리거맵 (커넥터 7개 전수 실측)

| 트리거 | from → to | Master |
|---|---|---|
| ◇신규 유저 | 진입 → 홈(전체노출형) | ✗ 행그룹 |
| ◇무료체험 5일째(시간조건) | 전체노출형 → 추천형-튜토리얼 | ✗ 트리거 없음 |
| **"카드 고르기" 필 버튼** | 홈(추천형) → 커스텀(디폴트) | ✓ |
| "추천학습으로 돌아가기" 버튼 | 커스텀(추가제거) → 초기화 모달 | ✗ 부모↔모달 §9 |
| **"저장하기" cta** | 커스텀(추가제거) → 커스텀형-설정완료 | ✓ |
| **모달 확인 버튼** | 초기화 모달 → 추천형-설정완료 | ✓ §9 |
| ◇기존 유저 | 진입 → 공지 모달 | ✗ 행그룹 |

## 검증

- 수치: 섹션 위치·밴드 x/폭·DEPTH_GAP [800,800,800]·행간격 [837,837]·도착 center 3/3·관통 0·섹션 겹침 0 — **전항 통과**
- verifyRowBranching: 위반 없음
- 의미(시각 대조): **커넥터#1 트리거 오류 발견·수정** ↓

## 🐛 빌더 갭 발견 — 우측끝아이콘 규칙이 pill 버튼 트리거 오인

- connect-builder `findTrigger`가 "카드 고르기" 텍스트로 행을 식별한 뒤 **행 우측 끝 book 아이콘**(다른 기능)을 출발점으로 선택. 수치검증(첫꺾임·도착·관통) 전항 통과 — **의미 오류는 시각 대조만이 잡음** (챌린지 arrow_right와 동일 뿌리).
- 원인: §6 메뉴행 규칙(라벨→우측 아이콘)은 *소스가 아이콘에서 출발*한 케이스 전제. 여기선 소스 네이티브 커넥터가 **"카드 고르기" pill 버튼 자체**(소스 6423,1307)에서 출발 → 독립 버튼인데 메뉴행으로 오인.
- 조치: 해당 커넥터만 pill 버튼 우측(4167,1715) 출발로 수동 재생성(mkVec 동일 스타일, Z-경로, 검증 재통과).
- **TODO(가이드 개정 후보)**: findTrigger에 "소스 네이티브 출발점과 최근접 요소 우선" 옵션 or pill/독립 버튼(아이콘+라벨 묶음) 판별 추가. 태블릿(1280)에선 아이콘 필터 상수(0.3W=384, RE−70)도 재점검.

## 🐛 빌더 갭 2 — 텍스트 폴백 출발 (사용자 지적으로 수정, 같은 날)

- #2(저장하기)·#3(모달 확인)이 via '요소자체(폴백)' = **버튼 내부 텍스트 우측**에서 출발 (버튼 모서리와 170px/27px 차). §5 원칙(출발=트리거 컴포넌트 bbox 모서리) 위반.
- **원인**: findTrigger 판정 상수가 모바일(375) 비율 기준 — 태블릿(1280)에서 CTA(397px)가 규칙1(>0.55W=704 탈락)·규칙2(<0.3W=384 탈락, 13px 차)·규칙3(>0.5W=640 탈락) 전부 미달 → 텍스트 폴백. 갭1(pill 오인)과 같은 뿌리 = **상대비율 상수 vs 태블릿 절대 크기**.
- **조치**: 텍스트→가장 가까운 버튼형 조상(INSTANCE, h≤80) 상승 → 그 우측 모서리 출발로 재생성. #2 (8440,2087) cta / #3 (6700,3517) tablet-button. 첫꺾임·도착 center 재통과.
- **✅ 개정 반영 (같은 날)**: connect-builder.js에 **규칙1.5** 추가 — 텍스트 매치 시 좁은 버튼형 조상(INSTANCE/COMPONENT, h≤80, 폭<0.5W) 우측에서 출발. 넓은 메뉴행(≥0.5W)은 건너뛰어 기존 우측끝아이콘 동작 보존.
  - **검증**: RA 커넥터 3개를 개정 빌더로 통째 재빌드 → 전부 `via: 버튼조상`, 출발점이 수동 수정과 정확히 일치 (4167,1715 / 8440,2087 / 6700,3517), 첫꺾임·도착·관통 ok. **갭1(pill 오인)·갭2(텍스트 폴백) 모두 규칙1.5 하나로 해소** — 카드 고르기도 title 인스턴스(347×32) 조상이 잡힘.
  - **SSOT 동기화**: Figma `16097:3930` 트리거 인식 블록에 규칙1.5 추가 (+114px, 하위 60개 노드 cascade shift, 섹션 10646→11006 리사이즈, 겹침 0).
