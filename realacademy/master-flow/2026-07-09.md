# 2026-07-09 — 판단기호(다이아) 시각 확인 오류 교정 + 가이드 보강

> 리아 학습커스텀 플로우(`50686:3312`) 세그먼트 재검증 계기. Figma 변경 없음(디자인·Master 모두 이미 정확) — **가이드 규칙만 보강.**

## 발단

사용자: "홈_추천형 맨 앞 '무료 체험 5일째 날'이 다이아 안에 든 거 보여? 이럴 때 유저 타입으로 나누기로 했잖아."

- 내 1차 오류: "무료체험5일째 = 시간조건이니 상태"라고 **의미로 넘겨짚음**(마커 우선 원칙 위반).
- 내 2차 오류: 다이아 탐지를 **조상만·이름/POLYGON 휴리스틱**으로 → 다이아가 텍스트의 **형제 Vector**라 못 잡고 "다이아 아님" 단정. 게다가 **스크린샷 도구가 불안정**해 시각 확인을 못 한 채 프로그램 판정만으로 결론.

## 재검증 (시각+구조 제대로)

`node.screenshot()` 우회로 시각 확인 → 소스 다이아 3개 실측:
| 다이아 | 위치 | 판정 |
|---|---|---|
| 신규 유저 | x558 (맨 좌측 시작점) | **유저 세그먼트** |
| 기존 유저 | x558 (맨 좌측 시작점) | **유저 세그먼트** |
| 무료 체험 5일째 날 | x4113 (내부) | 화면 조건, 세그먼트 아님 |

→ 사용자 확정 규칙: **"유저타입 다이아 = 플로우 맨 처음 시작점만."** 신규/기존이 세그먼트, 무료체험5일째는 내부 조건. **원래 판단(신규/기존)이 맞았고 §2-3도 이미 맞음** — 순전히 내 다이아 탐지 실패였음.

- 현재 Master 시각 확인: D1 "홈 (신규 유저)" / Row2 "공지 모달 (기존 유저)" — **세그먼트 라벨 이미 정확, 재정리 불필요.** 무료체험5일째는 §5대로 미표기.

## 가이드 보강 (`_shared/master-flow/guide.md`)

1. **§2-3**: ⓐ 다이아=무조건 인식(문맥 오버라이드 금지), ⓑ **다이아 탐지 시각 확인 필수**(형제 Vector·회전사각형 가능, 프로그램만으로 '없음' 단정 금지), ⓒ 시작점=세그먼트 / 내부=화면조건 명확화.
2. **작업순서가드**: ⭐ **시각 확인 필수 — 프로그램 판정 ≠ 시각 판정** (다이아·트리거 의미·행/세그먼트 배치·화면 식별). 스크린샷 실패 시 `node.screenshot()` 우회, 끝내 못 하면 '미확인'으로 남기고 진행 ❌.

## 교훈

수치검증이 *의미* 오류를 못 잡듯, **프로그램 판정은 *시각* 오류를 못 잡는다.** 도구(스크린샷)가 실패했을 때 프로그램 판정으로 갈음하면 안 됨 → 우회해서라도 눈으로 확인. [[feedback_rule_in_worksteps]]

## 추가 (같은 날) — 세그먼트 마커 규칙: 위치 → 모양 기반 개정

사용자가 디자인·규칙을 갱신:
- **⬜ 사각형+텍스트 = 유저 세그먼트** (항상 시작점, 자기 세그먼트 첫 화면 왼쪽). 세그먼트 여러 개 가능.
- **◇ 다이아+텍스트 = 화면 분기점** (내부 조건, §5대로 Master 미표기).
- 구 규칙(다이아 위치 D1좌측=세그먼트/내부=분기)은 폐기.

리아 학습커스텀 소스 실측(시각 확인): 마커 3개 전부 **사각형**으로 교체됨 — **신규 유저**(x603, 전체노출형) / **가입 5일 후**(x4125, 추천형→커스텀→설정완료, 구 "무료체험5일째"에서 개명) / **기존 유저**(x619, 공지모달). fill+stroke FRAME, cornerRadius 0, 회전 0 — 확대 스샷으로 사각형 확인.

- **디자인 문제 없음** (3 세그먼트 일관).
- **Master 재구성은 사용자 지시로 보류** (전체노출형/추천형이 이제 다른 세그먼트지만 학습커스텀 `50686:3312`은 현행 유지).
- 반영: `_shared/master-flow/guide.md` §2-3 모양 기반 재작성 + §9 시각검증 문구 + `.claude/skills/master-flow-new/SKILL.md` 0단계 마커 설명.
